NuGet.VisualStudio.Interop.dll is included here to be able to reference it
and to base our wizard on it. However, it is not packaged in the VSIX,
so we're still using the Visual Studio version of this file instead of
the one that's here.